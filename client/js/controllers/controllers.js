'use strict';

$('document').ready(function () {

    // Sorting variables
    const sortCategory = $('.select2-selection__rendered').attr('title');
    console.log(sortCategory);
    const sortPriceRange = $('');

    // Filter variables
    const price = null;
    const color = null;


});